[pytest]
DJANGO_SETTINGS_MODULE = MacFAST.settings_test
pythonpath = .
testpaths = core/tests
collect_imported_tests = false
addopts = -m "not performance"
markers =
    performance: marks tests as performance benchmarks (deselect with '-m "not performance"')
